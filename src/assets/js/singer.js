// Singer object
export default class Singer {
  constructor({ id, mid, name, avatar, key }) {
    this.id = id
    this.mid = mid
    this.name = name
    this.avatar = avatar
    this.key = key
  }
}
